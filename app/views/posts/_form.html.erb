<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tytul %><br>
    <%= f.text_area :tytul %>
  </div>
  <div class="field">
    <%= f.label :krotki_opis %><br>
    <%= f.text_area :krotki_opis %>
  </div>
  <div class="field">
    <%= f.label :dlugi_opis %><br>
    <%= f.cktext_area :dlugi_opis %>
  </div>
  <div class="field">
    <%= f.label :tagi %><br>
    <%= f.text_area :tagi %>
  </div>
  
    <div class="field">
		<%= f.label :cover %><br>
		<%= f.file_field :cover %>
  </div>
  
	<div class="field">
		<%= f.label :is_promotion %><br>
		<%= f.check_box :is_promotion, {}, "y", "n" %>
	</div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
