<%= form_for(@dish) do |f| %>
  <% if @dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nazwa %><br>
    <%= f.text_area :nazwa %>
  </div>
  
  <div class="field">
    <%= f.label :skladniki %><br>
    <%= f.text_area :skladniki %>
  </div>
  
    <div class="field">
		<%= f.label :rodzaj %><br>
		<%= f.collection_select(:rodzaj, Type.select(:rodzaj, :id), :id, :rodzaj) %>
		<%= link_to "Dodaj nową kategorię", {controller: "dishes", action: "add_category"} %>
		<%= link_to 'Edytuj kategorie', edytuj_kategorie_url %>
	</div>
	
	<div class="field">
		<%= f.label "Zdjecie" %><br>
		<%= f.file_field :icon %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
