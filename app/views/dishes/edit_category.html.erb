
<div class="edit_categories">
		<%= form_for @category, url: {controller: "dishes", action: "updatecategory"} do |f| %>
		  <% if @category.errors.any? %>
			<div id="error_explanation">
			  <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

			  <ul>
			  <% @category.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			  <% end %>
			  </ul>
			</div>
		  <% end %>

		  <%= f.hidden_field :id, :value => @category.id %>
		  
		  <div class="field">
			<%= f.label :nazwa %><br>
			<%= f.text_area :rodzaj %>
		  </div>
		  
		<div class="field">
			<%= f.label :visible %><br>
			<%= f.check_box :visible, {}, "y", "n" %>
		</div>
		  
	  <div class="actions">
		<%= f.submit %>
		 </div>
	<% end %>
</div>