<%= form_for(@slider), :remote => true do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tytul %><br>
    <%= f.text_area :tytul %>
  </div>
  <div class="field">
    <%= f.label :krotki_opis %><br>
    <%= f.text_area :krotki_opis %>
  </div>
  <div class="field">
    <%= f.label :artykul %><br>
    <%= f.collection_select(:artykul, Post.select(:tytul, :id), :id, :tytul) %>
  </div>
  
   <div class="field">
		<%= f.label :photo %><br>
		<%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
